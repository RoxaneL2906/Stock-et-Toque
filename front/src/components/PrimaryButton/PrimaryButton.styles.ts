import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  bouton: {
    backgroundColor: '#22C55E',
    borderRadius: 20,
    paddingVertical: 12,
    alignItems: 'center',
    width: 220,
  },
  texte: {
    color: '#000000',
    fontWeight: 'bold',
    fontSize: 14,
  },
});